{
  "name": "@cadlad/mcp-gateway",
  "version": "0.1.0",
  "description": "MCP server — assistant bridge for CadLad live sessions",
  "type": "module",
  "main": "dist/server.js",
  "bin": {
    "cadlad-mcp": "./dist/server.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "eslint .",
    "start": "node dist/server.js",
    "test": "node --test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.7.0"
  }
}
