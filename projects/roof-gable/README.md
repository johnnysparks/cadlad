# Roof Gable Strict Feature Demo

Demonstrates registry-backed strict scene features:

- `feature("wall.straight", ...)`
- `feature("roof.gable", ...)` with `hostId` compatibility checks

See `roof-gable.forge.ts` for a minimal example scene envelope and geometry build.
